name: todo
description: 'A new Flutter project.'
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flame: ^1.33.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # asset 이미지 허용
  assets:
    - assets/images/otter_sprite_pack/
    - assets/images/background/
    - assets/images/obstacle/
  #  파일 불러 올때 여기 등록 된 경로(폴더명)부터 시작해야함.
